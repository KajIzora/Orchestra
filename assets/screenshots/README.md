# Screenshots

Drop the main hero screenshot here as `hero.png`. The top of the project
[README](../../README.md) references `assets/screenshots/hero.png`.

Tip: capture the app with a few tasks in different live states (watching /
waiting / needs input / done) so the colored status dots show off the
glanceable view — that's the most compelling shot.
